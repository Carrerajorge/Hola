name: Publish Docker images (main)


on:

  push:

    branches: [main]

  workflow_dispatch:


permissions:

  contents: read

  packages: write


jobs:

  build_images:

    name: Build & push images to GHCR

    uses: ./.github/workflows/build-images.yml

    with:

      sha: ${{ github.sha }}
