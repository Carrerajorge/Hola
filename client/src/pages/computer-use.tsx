/**
 * Computer Use Page - Full browser + terminal control interface
 *
 * Wraps the ComputerUseDashboard component as a routable page.
 */

import { ComputerUseDashboard } from "@/components/computer-use/ComputerUseDashboard";

export default function ComputerUsePage() {
  return <ComputerUseDashboard />;
}
